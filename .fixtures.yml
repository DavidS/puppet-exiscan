fixtures:
    symlinks:
      exiscan: "#{source_dir}"
      tp: "#{source_dir}/../puppet-tp"
    forge_modules:
      concat: 'ripienaar/concat'
      postgresql: "example42/postgresql"
      puppi: "example42/puppi"
      stdlib: "puppetlabs/stdlib"
      tinydata: "example42/tinydata"
      # tp: "example42/tp"
